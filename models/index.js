const Server = require('./server_model');
const Usuario = require('./usuario_model');
const Nivel = require('./nivel_model');
const Role = require('./role_model');

module.exports = {
    Server, 
    Usuario,
    Nivel,
    Role
}